# Render worker dependencies only.
#
# Deliberately NOT requirements.txt. This container executes LLM-generated
# Python, so every library present is a library that code can reach. Absent
# here on purpose: pinecone, openai, elevenlabs, boto3, unstructured, pandas,
# fastapi. A render needs none of them, and their credentials are not in this
# container's environment either.

# ── Task queue ────────────────────────────────────────────────────────────────
celery==5.6.3
kombu==5.6.2
redis==8.1.0

# ── Supabase (media_assets rows + rendered-media storage) ────────────────────
supabase==2.31.0
postgrest==2.31.0
python-dotenv==1.2.3

# ── Scene generation ──────────────────────────────────────────────────────────
anthropic==1.0.0

# ── Animation ─────────────────────────────────────────────────────────────────
manim==0.21.0
